test_name: Validar API de produtos

stages:
  - name: Buscar produtos

    request:
      url: http://localhost:8000/api/produtos
      method: GET

    response:
      status_code: 200

      json:
        - nome: !anystr
          preco: !anyfloat
          estoque: !anyint

        - nome: !anystr
          preco: !anyfloat
          estoque: !anyint